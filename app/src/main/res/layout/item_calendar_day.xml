<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/day_container"
    android:layout_width="70dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingVertical="8dp"
    android:layout_marginHorizontal="4dp"
    tools:background="@drawable/rounded_day_pillar_selected"
    >

    <!-- Día de la Semana (Lun, Mar, etc.) -->
    <TextView
        android:id="@+id/text_day_of_week"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/black"
        android:textStyle="bold"
        tools:text="LUN" />

    <!-- Número del Día (1, 2, 24, etc.) -->
    <TextView
        android:id="@+id/text_day_number"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:gravity="center"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginTop="4dp"
        tools:text="24"
        tools:background="@drawable/rounded_selection_circle_active"
        />

</LinearLayout>